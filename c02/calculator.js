// Lokalen modul

const MAX_NUMBER = 99;

function calculator(operacija, a, b) {
  // op = "sobiranje"
  // op = "+"
  if (operacija === "sobiranje") return a + b;
}

// module.exports = calculator; // na ovoj nacin davame pristap na drugite fajlovi do ovaa funkcija
